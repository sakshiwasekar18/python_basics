#************************** READ IN CSV ***************************

import csv

# with open(r"C:\Users\91862\Desktop\SAKSHI_PYTHON\file_handling\student.csv","r") as file:
''' read=csv.reader(file)
  for row in read:   # you have to use foe loop to read  if u are not making a list of data and excessing directly

    print(row)'''
 '''read = csv.reader(file)
 for col in read:
  print(col[1])   # to read a specific column by index'''


  
  '''read_file= csv.reader(file)
  next(read_file)   #skips the reader 
  for row in read_file:
    print(row)'''
 

  '''reader=list(csv.reader(file))
  print(reader[1])    # as we have now converted  csv data into list  we can read by index
  print(" ".join(reader[2]))     #read data at list index two and join them seperated by a space ''' 
 


 

